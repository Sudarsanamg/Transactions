import React from 'react'
import { SafeAreaView, SafeAreaViewBase, StyleSheet, Text, View } from 'react-native';
const Titlebar = () => {
  return (
    <Text style={{marginTop:30,fontSize:30}}>Transactions</Text>
  )
}

export default Titlebar